# Style Dictionary Configuration

This directory contains all the configuration for Style Dictionary, the tool used to transform design tokens into platform-specific formats.

- `config.js`: The main configuration file defining platforms and transforms.
- `build.js`: A script to programmatically run Style Dictionary.
- `transforms/`: Custom value transformers.
- `formats/`: Custom output formatters.