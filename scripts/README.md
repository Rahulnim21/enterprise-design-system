# Monorepo Scripts

This directory contains custom operational scripts for the monorepo. These scripts handle tasks that are too complex for a simple `package.json` entry.

- `generate-tokens.js`: Runs the Style Dictionary build process.
- `generate-icons.js`: Processes SVG files into React components.
- `publish.js`: A custom script to handle the package publishing process.