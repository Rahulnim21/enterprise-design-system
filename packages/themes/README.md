# @enterprise/themes

This package provides theme configurations for the design system. It defines collections of design tokens for different themes, such as light mode, dark mode, or high-contrast mode.

These themes are consumed by the UI components to enable dynamic styling.