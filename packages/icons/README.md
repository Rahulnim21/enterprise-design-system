# @enterprise/icons

This package contains the icon library for the Enterprise Design System.

Icons are sourced as SVGs and automatically processed into optimized React components via a build script.