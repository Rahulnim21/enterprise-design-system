# @enterprise/hooks

This package contains a collection of shared, reusable React hooks that provide common logic for UI components and applications.

Examples might include `useBreakpoint`, `useTheme`, or `useId`.