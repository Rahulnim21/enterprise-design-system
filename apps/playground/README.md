# Playground

This application serves as a simple sandbox or "playground" for developers to quickly experiment with components from the `@enterprise/ui` library in a clean environment.

It is typically built with Vite for fast startup times.